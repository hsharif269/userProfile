spring:
  application:
    name: userProfile
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

server:
  port: 8082
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka